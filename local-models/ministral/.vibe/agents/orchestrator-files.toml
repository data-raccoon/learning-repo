agent_type = "agent"
display_name = "Orchestrator Files"
description = "Minimal file-only worker for bounded orchestrator jobs."
safety = "safe"
active_model = "mistral-medium-3.5"
system_prompt_id = "orchestrator-bounded"
enabled_tools = ["read_file", "grep", "edit", "write_file"]
disabled_skills = ["*"]
enable_connectors = false
include_project_context = false

[tools.read_file]
permission = "always"

[tools.grep]
permission = "always"

[tools.edit]
permission = "ask"

[tools.write_file]
permission = "ask"

