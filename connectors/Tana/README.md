# Minimal Tana import

Copy the complete contents of `minimal-tana-paste.txt`, then paste them into an
empty node in Tana. Tana recognizes the `%%tana%%` header and creates the nested
nodes and checkbox.

This is intentionally a small smoke test. Tana Paste creates new content; it
does not update existing nodes.
