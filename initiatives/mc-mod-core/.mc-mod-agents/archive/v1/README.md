# Archived v1 worker evidence

This directory preserves unsupported version-1 tasks, packets,
acknowledgements, and results. They are historical evidence only and must not be
executed, adapted, or treated as current release gates.

Create new work as v2 tasks through `model-execution-harness/core/harness.py`.
Current slice status is recorded in `.mc-mod-agents/STATUS.md`.
