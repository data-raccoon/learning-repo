[[providers]]
id = "local-colibri"
display_name = "Local Colibri (GLM-5.2)"
kind = "openai-compatible-local"
status = "deferred"
auth_kind = "external-file"
auth_env = "COLIBRI_API_KEY"
base_url = "http://127.0.0.1:8082/v1"
billing = "local-compute"
usage_reporting = "unavailable"
notes = "Pure C zero-dependency MoE inference engine. Streaming experts from disk. Loopback-only runtime; key and weights remain outside the repository. DEACTIVATED: Too slow for current use - POC only."
