Implement the complete `styles.css` for Glimweave using the supplied experiment identity,
game design, fixed HTML, and actual generated UI source. Return CSS only, without Markdown
or commentary.

Create an original aurora-workshop visual system rather than a generic dashboard:
- the canvas and interface should feel like one living Sky Loom instrument;
- four phases must have distinct but coherent atmosphere via state classes/data attributes;
- production, capture, fade, pressure, overflow, capacity, doctrines, Retuning, tutorial,
  offline report, settings, and victory must be visually legible;
- style every class/id emitted by `src/ui.js`, including cards, meters, costs, locked,
  affordable, purchased, selected, disabled, modal, tooltip, tabs/sections, and controls;
- responsive asymmetric desktop composition and a usable single-column 360px layout;
- strong focus-visible states, skip link, screen-reader utility, non-color cues, minimum
  touch targets, readable contrast, and colorblind-mode selectors;
- prefers-reduced-motion and explicit reduced-motion class must remove nonessential motion;
- use system fonts and procedural CSS only: no URLs, imports, external assets, or fonts.

Avoid excessive glassmorphism, tiny text, layout overflow, generic Bootstrap styling,
generated textual content, TODOs, and placeholder rules.
