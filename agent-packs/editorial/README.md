# Editorial agent pack

This portable pack contains five model- and IDE-neutral editorial agent
definitions plus shared selection, evidence, and reporting rules.

The Markdown files under `agent-definitions/` are inert role definitions. A
controller or execution harness selects one definition and supplies the model,
source material, tools, permissions, and output target at run time.
