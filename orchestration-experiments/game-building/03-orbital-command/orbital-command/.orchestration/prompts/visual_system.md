Create the complete `styles.css` for the supplied Orbital Command HTML and DOM contract.
Return CSS only without Markdown fences or commentary. Make it feel like a sophisticated
deep-space command console: readable, dense but calm, responsive, and visually distinct
from the earlier games. Style resource bar, asymmetric command grid, panels, cards,
buttons, progress indicators, dialog, banner, log, disabled states, hover, and strong
:focus-visible states. Include `.sr-only`. At widths below 760px use a single-column
layout. No URLs, imports, external fonts, images, or generated content required for
meaning. Respect prefers-reduced-motion.
