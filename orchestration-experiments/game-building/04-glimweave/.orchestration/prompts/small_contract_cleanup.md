Make exactly two localized edits and no others:

1. In `04-glimweave/index.html`, remove the second duplicate
   `<script src="src/test-bridge.js"></script>` while retaining the first.
2. In `04-glimweave/src/ui.js`, read only the `TEST.testRetuning`
   function near lines 1500-1520 and change its final Reservoir expectation from zero to
   the deliberate post-Retuning priming charge of 100. Keep its requirements for zero
   Weftlings, zero `totalGlimCapturedThisRun`, and increased Iridescence.

This overrides full-file reading. Use targeted reads, edit on the next turn, verify the two
regions, and return exactly two bullets. Do not run unrelated tests or commit.
