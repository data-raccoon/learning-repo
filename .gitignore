**/__pycache__/
*.py[cod]

# Model-orchestrator runtime evidence, snapshots, locks, and trajectories.
# Runtime directory can be outside repository via AGENT_ORCHESTRATOR_RUNTIME env var.
# Legacy location (kept for backwards compatibility):
agent-orchestrator/.runtime/

# Vibe runtime sidecars generated when the project-local provider config is used headlessly.
local-models/ministral/.vibe/connector_bootstrap_cache.json
local-models/ministral/.vibe/trusted_folders.toml
local-models/ministral/.vibe/vibehistory

# Cline task-queue handoff directory (managed by agent-orchestrator).
.cline-jobs/

# Generated LaTeX output.
*.aux
*.fdb_latexmk
*.fls
*.log
*.out
*.pdf
*.synctex.gz
*.toc
*.xdv
